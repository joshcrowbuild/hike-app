"""Code-orchestrated engine + provider seam + live adapters (Stage 4).

No agent framework: the Scout -> Verifier -> Curator flow is a fixed DAG we own
(design/runtime separation). See docs/research/stage-4-engine-and-cost.md.
"""
