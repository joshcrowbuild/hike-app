# frontend/

Reserved (not built yet). Per CLAUDE.md: **web/PWA first** (server-side watch
pull means minimal loss vs. native), **native iOS (SwiftUI) later** (Stage 11).

The deep experience + design system work — feed/card anatomy, confidence/
staleness as first-class UI states, the anonymous-browsing path, the calm-utility
aesthetic — is **Stage 10**, designed against the system's real behavior rather
than imagined. Nothing here until there's a slice to put a surface on.
