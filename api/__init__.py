"""HTTP / API surface — TBD.

A thin layer over orchestration.engine, built when a usable surface is needed
(web / PWA first, per CLAUDE.md). Anonymous browsing of the world + live
conditions is a real product surface (auth boundary = the shared/private boundary).
"""
