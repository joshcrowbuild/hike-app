# Roadmap — Adventure Planner

*Living status doc. Owned by the PM/planner lane. Terse by design — delete stale entries, wrong memory is worse than none (CLAUDE.md).*

**Last verified:** 2026-07-14 · **Owner:** PO (product-owner lane) · **Repo:** `joshcrowbuild/hike-app` · **Baseline:** tag `v0.1-phase1` @ `c12be36` · `main` is the protected line · **Version:** v21

> **◆ PHASE-C FRONT DOOR SHIPPED + DECISIONS LOCKED (v21, 2026-07-14).** The accounts chunk's first build landed: **Epic 043 managed auth DONE** (PR #235 — Supabase JWT verified at the edge against the public JWKS, `sub`→`viewer_id`; **anonymous browsing preserved by construction** with a regression test; shared dev-secret retired behind an off-by-default flag; first authed write `POST /episode` through the Epic-011 scoped seam; 1760 backend + 538 frontend tests green; self-review clean). Built ES256 (the live project signs EC P-256, corrected mid-build) with a mixed ES256/RS256 hermetic keyset. **Merge-safe with zero new env** — the live app keeps serving anonymous until the owner sets env. **Owner cutover PENDING (the only thing between here and live auth):** Render `SUPABASE_URL`; Vercel `VITE_SUPABASE_URL` + `VITE_SUPABASE_ANON_KEY`, unset `VITE_DEV_VIEWER_SECRET`; Supabase Site/Redirect URL; then the live sign-in check (043 DoD's last item). **Decisions locked this session:** §47 Phase-C pivot + Supabase · §48 running imports fully with per-activity capability channels (run pace never sets a hiking estimate) · §49 data-rights positions (immediate hard delete · no biometrics v1 · invite-only · commons opt-in) · **§50 map-matching (Open Decision #10 closed after a PO independent-verification pass): trajectory-similarity MVP (buffer + Hausdorff/Fréchet, real confidence gradient) + never-blocks fallback; Leuven HMM (Apache-2.0) vendored + deferred to the topology gate.** **Antigravity lanes delivered:** the Call-card design spec + Storybook slice (#232, 2 adoption flags), the map-matching borrow plan (#234), and the ToS/Privacy/promise-map drafts (#236 — 2 must-fix accuracy corrections before adoption: P4's biometric misstatement + a Rule-#4 miscitation). Epics **042 + 044 DEFINED** with full ACs (044 carries S6 corpus-grade import gates + §48 channels + §49 no-biometrics). **Next unblocked build:** the ToS/consent + data-export/deletion scaffold (the 8 [VERIFY] promises in the promise-map; gates 044) → 042 manual log → 044 import → 006 novelty.

> **◆ FORK RESOLVED → PHASE C: ACCOUNTS & TRIP MEMORY (v20, 2026-07-14).** Josh chose the **Phase-C pivot** over Phase B's remainder — rationale: years of Garmin history exist, and importing them is the most direct test of whether personal intelligence augments the experience (decision-log §47). **Supabase Auth signed off** (Open Decision #4 closed per `../research/auth-provider-decision-brief.md`). Epics: **043 managed auth (DEFINED, buildable now)** · **042 manual trip log (BACKLOG→DEFINED)** · **044 history import (DEFINED — build-gated on the pulled-forward ToS/consent + data-export/deletion line item; the deletion right precedes health-adjacent intake)**. Build order: 043 → 042 → ToS/deletion scaffold (needs the 15-min policy chat) → 044 → 006 novelty (unblocked by 044's `been_on` producer). Carried wrinkles, stated openly: map-matching may under-produce `been_on` (fallback = free-floating episodes, Open Decision #10), and hosted personalized ranking still degrades (the C4 local-judge rule vs. no local model on Render, PR #70) — a per-role model-routing decision is owed before the payoff is visible on the hosted app. The UX-vision adoption lanes can run in parallel.

> **◆ OWNER-STEWARD SESSION: RED-MAIN FIX → HONESTY RE-TUNE → M10 CLOSED → ELEVATION TRUTH → OMNIBOX → UX WAVE (v19, 2026-07-14).** One owner-supervised integration-steward session merged **#211–#224** (14 PRs) off a red `main` and left it green + reconciled. **Unbroke `main`** (#211): two `docs:`-labelled commits had shipped code that reddened required checks (a `pentest_live` urlopen missing `# nosec B310`; a prune-count integration test whose `_seed_trails` assumption made `deleted==20` not `5`) — both slipped past local `make check` (security-only + neo4j-only). **Confidence honesty model re-tuned** (#197/#215 · CDP-06): fusion is now weakest-link `min(a,f,c)`, and a `source_kind` primary/aggregated split (fail-closed default) lets an **authoritative single source reach "stated"** (verified live: the NWS line renders `stated`) while aggregated (AirNow) + single-origin corpus stay `hedged` and stale stays `flagged` — supersedes any "feed shows no confidence" claim. **M10 CLOSED** (#216): `scripts/lint_owned_reads.py` is now join-aware, a runtime `assert_scoped_read` guard sits in `ScopedSession.run`, all 11 `# noqa` removed — Rule #4 enforced by construction, plus a fixed hidden unscoped read in `watch_sync.py`. **Elevation truth**: the multi-part **seam-fix** (#214 — phantom bridge climbs inflating duration 2–5× on 12 trails) + the **DEM-clip buffer** (#217 — bbox-edge nulls; 0.05° margin) shipped and were **re-ingested to Aura** for PWF/Douthat/Shenandoah/St-John — corpus **2,622 trails / 99.1% elevation** (residual 23 nulls / 0.9%, thinly spread; a plus-fix on the coverage-gate's non-distinct denominator). **Omnibox / trail-name search SHIPPED** (Epic 038 build lane, Problem A — #218/#219, relevance tightened #221): a FULLTEXT `trail_name_fts` index + `scout_by_name` + a behaviour-preserving `_plan_from_candidates` extraction + `POST /search {query,k?}→FeedResponse` (relevance-ordered, honest empty) + an a11y-labelled Home search line — live end-to-end ("old rag" → Old Rag Loop/Fire Road/Access Trail). **Operational tail handled:** the `trail_name_fts` index was created on Aura by hand (the API doesn't auto-apply schema). Deferred: Problem B place-geocoding (provider decision, Open Decision #3), the CoMaps name-scorer (S2), and the *full* unified intent line. **UX wave:** a live multimodal review → HIGH fixes (card facts-row overflow + a calm 34rem desktop column, #220) + MEDIUM fixes (Detail conditions de-dup, honest null-elevation state, input signposting, #223). **A UX vision was commissioned** (Gemini via Antigravity, #222 — `docs/research/ux-vision-2026-07.md`: *"The Confident Call + Quiet Context"* + an isolated `VisionPrototype` Storybook slice) and its **first adoption lane shipped**: the **Context Ribbon** (#224) unifies Home's context sentence + the shared-conditions band into one honest landmark (per-trail readings never hoist; a region-level weather/AQI line is flagged as a backend follow-up, not faked). **Fleet cleanup:** 36 worktrees → 1, 185 → 1 local branch, all remote branches pruned. Verified live across **Vercel → Render → Aura**. **Open next (PO):** the vision's remaining adoption lanes (the "Call" hero card · the desktop map-split · a backend region-conditions probe); the **strategic fork still awaits the call** — Phase B (golden-trip cassette CI gate; source-or-silence is undefended on PRs) vs. Phase C (Supabase auth + episode intake). Reusable Antigravity design brief lives at `docs/research/ux-vision-brief.md`.

> **◆ PHASE-1 BASELINE + DOCS OVERHAUL — both complete; `main` is the single line.** Phase-1 build is done (backend personalization + design-system + the personal-intelligence app UX, all verified honest) and collapsed onto `main`, tagged `v0.1-phase1`. Repo is on the `joshcrowbuild` Team org; `main` is the default + protected branch with **7 required CI checks**: `format-check` · `lint` · `typecheck` · `test` · `integration (neo4j)` · `workflow-lint` · `docs-lint`. No force-push/delete; admin override retained. All merged feature branches swept (only `main` remains remote). **The documentation overhaul is complete** (PRs #27–30): 55 live docs (down from 60), ~115KB of closed history archived, 0 broken links, a lean always-load hot path, and a CI `docs-lint` gate that **fails the build on wrong memory** (stale-marker denylist · broken-link check · auto-generated epic index). **Go-forward model:** feature branches off `main`; lanes are conventions, not long-lived branches.

> **◆ HOSTED & LIVE — dogfood #1 CLOSED; the primary surface now serves clean data (v11).** Full stack deployed: Vercel → Render API → Aura; maps+terrain (016/017) serve real geometry + 3DEP elevation; Cypher-25/provider/TLS fixed (#41/#44). **This round:** the corpus-filter fix landed (#56 — a ref-keyed disqualifier for TIGER-mis-imported numbered state routes, a `wellness` token for the institutional-footway class, and a stale-node prune in `graph/load.py`), and the overnight re-ingest applied it (corpus 1532 → **1481**, the right direction). **Verified on live /plan near Front Royal (38.918,-78.194):** no SR/CR state routes, no "Snake Road"/"Little Loop Road"/"Andreae" — and real trails serve (Dickey Ridge, Fox Hollow, Snead Farm Loop, Front Royal Connector), with legit fire roads (Compton Gap Road, `ref=None`) correctly kept. Cold-start fixed (#52) and **Render upgraded to the always-on paid tier — instance no longer idles, cold-start eliminated**; /plan secured (#53), corroboration wired (#54), API tests merged (#57). **Live-reliability finding (2026-07-01):** /plan intermittently 500'd on a first-after-idle request (dogfood: 1 fail → then 12/12 incl. 6 concurrent = 200); always-on + the 5s `/health` graph-probe now keep the Aura pool warm, and a hardening lane (`claude/plan-reliability`, in flight) adds driver + provider transient-retry so a blip self-heals. **New watch item — candidate dogfood #2, NOT the systemic TIGER class:** residential-style "Lane" names survive (Turkey Lane, Yard Bird Lane) — they carry no SR/CR `ref`, so they're outside #56's filter and need a separate signal. Rule still holds: **substrate before surface.** Verdict: honestly-good on the core surface; now iterate the long tail.

> **◆ OSS-BORROW WAVE + LATENCY WAVE-1 LANDED; DASHBOARD RECONCILED (v12, 2026-07-11).** ~30 PRs merged since v11 (#112–#155). The CoMaps-borrow build wave shipped: 022 duration-truth · 023 agency-length · 024 schema-format · 025 validity-exclusions · 027 facet ingest-diff · **030 slug-collision guard + re-runnable audit (the Phase-A landmine — closed)** · 031 GPX reader · 032 grade-aware ETA · 033 OpenTopoData fallback · 034 NPS closures adapter · 035 OSM water overlay · 036 pyosmium transport · **028 GPX export** (endpoint + Send-to-device; live endpoint verified serving well-formed GPX 1.1). Also: 038 search spike (B001, spec-complete) · 037 Open-Meteo failover spike (**downstream build BLOCKED on a PO commercial-license decision**) · 039 latency Wave 1 (returning paint 0.385s) · **B010 trail-connectivity research + spike (#155, parked at Phase E, `ready`)**. Corpus after the wave's re-ingests: see `STATUS.md` (refreshed 2026-07-11 — counts live there per G4; headline: ~2.2k trails, schema 0.2.0). **Live /plan re-verified 2026-07-11 (anonymous, Front Royal):** 8 real cards, each carrying sourced+timestamped NWS weather + USGS gauge lines with honest single-source labels; no TIGER junk and no residential-"Lane" names observed in the top-8 (a *targeted* dogfood-#2 check is still owed before calling that class closed). **Drift fixed this pass:** 016/017/028 epic headers flipped to DONE (they had shipped; statuses never flipped — index regenerated). The v11 "cards thin/anonymous (`lines:[]`)" claim is obsolete — cards are no longer thin; what remains of that thread is Epic 018's *completeness* (four-state silence + closures/AQI/fire kinds on the card — much of its substrate shipped via 034/035, so 018 needs a scope reconcile before build). The `claude/plan-reliability` lane's branch no longer exists on origin and its outcome is unverified in this pass — treat "in flight" mentions below as historical.

> **◆ OVERNIGHT PROGRAM + MERGE TRAIN + LATENCY TRUTH (v18, 2026-07-13).** One owner-supervised overnight session (PO "po-RC") produced **14 merged PRs (#172–#185)**: Epics **018 (keyed sources), 040 (two-phase render), 041 (water overlay read surface) all DONE**; two adversarial design reviews shipped (**#174** condition-surface honesty, 12 findings · **#179** craft layer, 14 measured findings) with their first fix lanes already merged (**#181** one-sky-one-verdict — card/Detail verdict agreement now holds by construction + eval-gated; **#183** sheet mechanics/boot shell/tuning persistence); infra hardened (**#177** prune now self-heals filter drift via per-run ingest markers — the constant-`ingest_version` blindness that forced a manual 10-node junk purge is closed · **#180** the `ADVENTURE_REGION`-vs-`.env` make footgun · **#185** shared HTTP transport). **Corpus: 2,197 → 2,664 trails / 16 regions** — first ingests for first-landing, PWF, Westmoreland, York River, Harpers Ferry, Catoctin-Sugarloaf, and **St. John USVI (non-CONUS stress test passed end-to-end: real 3DEP elevation, NPS conflation, honest condition degradation)**; zero guard overrides across the whole program; Aura Free usage ~5% of caps (the R5 cliff is far). **Latency truth found and fixed:** post-train cold `/plan` read 13–21 s; root cause was **per-probe TLS-client construction (~350 ms GIL-serialized CPU each, #185) on an instance that was actually Render FREE (0.1 vCPU) — the "always-on paid" claim below (v11) was stale; wrong memory, corrected here.** After #185 + a real **Starter upgrade** + judgment-tier fast curate (`claude-haiku-4-5-20251001`, ranking spot-check clean): **cold single-shot 5.9–6.6 s (<8 s SLO passes) · phase-1 to-cards 2.6 s · warm ≈0.2–1.2 s** (full verdict in Epic 040's DoD). **Open follow-ups:** A2 per-role model seam (lane in flight — returns the eval judge to Sonnet); craft Lanes B/C + #174 Lanes 2–4 (owner green-light); PWF small-region elevation-coverage-bar design call; browser-protocol <1.5 s p75 pass after A2.

> **◆ OWNER OPS DAY + SECURITY REMEDIATION (v17, 2026-07-12, PR #166 + dashboard actions).** The owner executed the full dashboard queue in one sitting: **warmer OFF** (`ADVENTURE_FEED_WARM_INTERVAL_S=0`, confirmed in startup logs — the $20–470/mo band collapses to organic ~$1–2/mo; Epic 040 remains the proper fix) · TTL 600 + probe workers 16 (**cold /plan re-measured: 7.2s worst / 4.7s typical vs 14.4s prior, now probing six kinds — <8s SLO passes**) · **branch protection: 12 required checks** · **all four agency keys live** (AirNow/FIRMS/RIDB/NPS + `ADVENTURE_LIVE_ADAPTERS` full list) — **live cards verified serving 5–6 condition kinds sourced+timestamped** (air initially `unavailable` from a 28s AirNow stall, honest couldn't-verify shown; watch for self-heal) · Vercel prod bundle grep-verified sample-free. **Security:** owner-commissioned third-party review (all 8 findings PO-verified accurate — `docs/research/security-review-2026-07-12.md`, stamped) + a PO-found leak (FIRMS/AirNow keys in httpx-logged URLs) **remediated same day in #166**: input caps, finite-env parsing, secret-URL redaction (live-fire verified against the actual leaked line), NWS SSRF pin, lockfile + **pip-audit blocking** — bandit/secret-scan/pip-audit now all enforce. PlanMetrics ground truth landed: **est_cost ≈ $0.0016/cold plan** (estimator undercounts prompt tokens; order of magnitude stands) → **R5 CLOSED**. Owner residuals: rotate the FIRMS key (leaked to old logs) · add `pip-audit` as the 13th required check · **run the corpus re-ingest** (runbook: `docs/runbooks/reingest-corpus-quality.md`) once PR #170 merges — dogfood #2 closed by that lane (reported Lane class already fixed; real gap was Circle/Place cul-de-sacs, six live, one outranking real trails; filter extended config-only, purge needs the re-ingest).

> **◆ LATENCY WAVE 4 MERGED + R5 COST MODEL WRITTEN (v16, 2026-07-12, PR #164 + this pass).** Wave 4 (PO-verified pre-merge; #164): **B5 default-frame warmer** (key-alignment verified end-to-end: query "weekend morning moderate" · k=10 · frontRoyal-first origin — primes exactly the fresh-tab key) · **B4 corroboration-read overlap** (maps half skipped with sound analysis — absorbed by Epic 040's phase split) · **Epic 040 two-phase render DEFINED** (second-call over SSE; pending state = `not_fetched`; three new gate predicates; S4 not relitigated) · self-review caught a CRITICAL (test suite could have spawned a real paid warm loop — stubbed + pinned). **⚠ THE WARMER IS NOW LIVE AND IS THE APP'S DOMINANT COST DRIVER AT DEFAULTS** — it re-primes all 11 region frames on a 240s cadence; bottom-up bands run **$20–470/mo depending on prod tier config** vs ~$1–7/mo organic LLM spend. Full model, mitigation ladder, and three cost postures: **`docs/research/r5-cost-model.md`** (new — closes R5's modeling half; one paste of a day's `PlanMetrics` Render log lines closes the measured half). Immediate owner lever: TTL→600 halves it; `ADVENTURE_FEED_WARM_INTERVAL_S=0` kills it; a small scope-to-served-region lane cuts frames 11→1; Epic 040 retires it. Post-merge cold-path re-measurement per the Wave-1 verdict protocol is owed after the env flips land.

> **◆ PHASE-B WAVE 3 LANDED & PO-VERIFIED (v15, 2026-07-12, PR #162).** Three lanes + one self-review fix: **Epic 018 honestly reconciled** (built stories struck with evidence; open scope = S4f→shipped this wave, S2 keyed sources, S6c cost read; header IN_PROGRESS) · **the six per-kind condition states render** on card + Detail (checked-clear = calm sourced silence; outage routes through the flagged couldn't-verify treatment so it can never read as quiet not-fetched; the `lines.length===0` mislabel retired, surviving only for payload-less older backends; 17/17 stories under the blocking a11y gate) · **the states are now gate-defended** — `condition_states` is a required hard key in eval-replay, with two new golden scenarios (`answered-clear-vs-outage`: three distinct silences in one bundle · `parse-failure-never-answers`: the #160 fabrication bug locked in as a permanent gate-level regression). The wave's self-review caught **1 CRITICAL**: the stale-while-revalidate repaint wasn't stripping `conditions`, so a failed refetch could serve a frozen sourced "checked — nothing to flag · 20m ago" hours later — the false-fresh CDP-08 failure, now stripped + schema-bumped + regression-tested. **PO verification:** CI 14/14 green on the PR; gate green locally across all 7 scenarios; the new check **falsified by hand** (one flipped pinned state reds the build naming card + kind; restore → green). Live not-fetched rows for air/fire/closures/permits flip to real dispositions once the owner provisions the free agency API keys (owner queue #4).

> **◆ PHASE-B WAVE 2 LANDED & PO-VERIFIED (v14, 2026-07-11, PR #160 — merged by the Integration Steward).** Four lanes: bandit triage → **blocking** (Highs annotated `usedforsecurity=False`, B310s scheme-audited) · **blocking a11y gate** over the honesty-primitive stories (falsification-checked: a planted alt-less image trips it) · the vanished plan-reliability thread **closed — it had shipped as PR #65 on 2026-07-01** (driver liveness + bounded retry + provider transient-retry, 29 hermetic tests; verification note in `docs/research/plan-reliability-verification.md`; the v12 ❓ is resolved, no code was missing) · **CDP-02 legible silence** — every card now carries a per-kind `conditions` array (`present | stale_degraded | no_hazard | no_data | unavailable | not_fetched`), adapters split "couldn't verify" from an *answered* empty, and the wave's self-review caught a real fabrication bug (an NPS parse failure was reading as a sourced "no coverage" — fixed with regression tests). **PO verification:** CI green on the PR head (except the item below); eval-replay gate green post-merge locally; **live prod card probed 2026-07-11: the `conditions` payload serves** (weather/water `present` with source+timestamp; air/fire/closures/permits honestly `not_fetched`). **One wave defect found + fixed in v14's PR:** the secret-scan lane flipped to `gitleaks/gitleaks-action@v2`, which hard-fails org accounts without a paid `GITLEAKS_LICENSE` (its red ✗ on #160 was the license gate, not a leak) — replaced with the free MIT gitleaks **CLI** (pinned + checksum-verified), same `.gitleaks.toml`; validated by this PR's own CI run (sandbox proxy can't fetch GitHub releases). **Known interim quirk (documented in #160):** the frontend still derives silence from `lines.length===0`, so an answered-clear card can read "couldn't be verified" until the silence-rendering lane ships — conservative direction, never a false clear.

> **◆ PHASE-B WAVE 1 LANDED & PO-VERIFIED (v13, 2026-07-11, PR #157).** Six lanes, one mobile-kicked Builder session: advisory security scanning (bandit/pip-audit/gitleaks — bandit High×3 + secret-scan pre-existing findings documented in the PR, jobs red-but-non-blocking until triaged) · frontend Vitest in CI with coverage · log hygiene (`orchestration/logsafe.py`: owner/episode-id scrub + API logging config) · sample store dev-gated out of production builds (grep-verified empty dist) · backup/DR runbook stub · **the source-or-silence regression gate** (`evals/replay.py` + 5 golden-trip scenarios + cassettes, hermetic ~1s, Epic 009 partial — header at IN_PROGRESS, judge/Brier/N-run deferred). **PO verification, not just trust:** CI all-green on the PR; the gate re-falsified by hand post-merge (a perturbed recorded fact reds the build with `fact_fidelity: recorded 99, surfaced 68`; restore → green). **⚠ THE GATE DOES NOT DEFEND YET:** `eval-replay (source-or-silence)` and `frontend (vitest)` run on every PR but are **not in the branch-protection required checks** (still the original 7) — until an admin adds them (repo Settings → Branches, needs web UI), a PR that reds them can still merge. That flip is the single Phase-B action outstanding that costs two minutes. Remaining Phase-B scope after it: ToS/privacy/consent + data export/deletion (**hard blocker for Phase-C history import**), Sentry/uptime (owner accounts), a11y baseline gate, R5 cost read from Render logs, bandit-High triage → flip security jobs to blocking.

> Companion to `docs/workplan.md` (the 11-stage agenda + threads T1–T7) and `docs/epics/README.md` (epic index — now generated from epic headers). This doc aggregates *live* state across lanes; the workplan is the plan, the index is the per-epic source of truth, this is the dashboard. New here? Start at `docs/README.md` (the doc map).

---

## TL;DR — where we are

**Hosted end-to-end; primary surface now serves clean data — dogfood #1 closed.** Phase-1 backend, design system, app UX, Maps (016/017) on `main`; plumbing works (Vercel → Render → Aura). The hard moat is built: invariant-faithful engine, CI-proven query-layer access control, provenance graph. **Phase A (now), pre-Milestone-1** per path-to-complete (#48). Phase-A status:
> - **Re-ingest** — DONE (corpus 1532 → 1481 after the #56 filter; TIGER state routes + the Andreae footway gone, verified on live /plan).
> - **Cold-start / API hosting** — DONE + hardened: #52 (60s timeout + "waking" UX) and **Render on Starter (0.5 vCPU — the "always-on paid" claim here was stale; the instance sat on Free until 2026-07-13, see v18)**, so the instance no longer idles and cold-start is eliminated (the warm-ping cron was throttled to every 2–5h on the private repo and never held it warm). R7 API-hosting resolved; only the watch-poller host stays deferred.
> - **Live reliability (2026-07-01, CLOSED)** — /plan intermittently 500'd on a first-after-idle request (1 fail → then 12/12 incl. 6 concurrent = 200). A stale Aura connection was the cause (Aura idle-closes; first query after idle throws before reconnect); always-on + the 5s `/health` graph-probe keep the pool warm, and the transient-retry hardening **shipped as PR #65** (driver liveness + bounded retry + provider transient-retry; verification note `docs/research/plan-reliability-verification.md`).
> - **Dogfood #1 (junk on /plan)** — CLOSED (#56 + re-ingest): ref-keyed disqualifier dropped the systemic TIGER state-route class; verified absent on live /plan near Front Royal, real trails + legit fire roads kept. **Long-tail follow-on (dogfood #2)** — INVESTIGATED + CLOSED (2026-07-12): the reported "Lane" names are in fact already dropped. The `residential_street_suffix` disqualifier (added with the spatial classifier, live since the 2026-07-07 re-ingest) catches every Lane/Court/Drive — live `/trail` 404s Turkey Lane, Yard Bird Lane, Skyway Lane, and an OSM-source replay of the ingest filter confirms the drop. But the *targeted* full-region sweep surfaced a real adjacent gap the top-k probe missed: the suffix set omitted **Circle/Place**, so 6 TIGER cul-de-sacs survived into the live corpus (Oak/Overlook/Peace and Quiet/Toms Dam/Bluebird Circle, Wildwood Place — all `/trail` 200), and Oak+Overlook Circle even reached the Front-Royal-area `/plan` top-10. Fixed by extending the config-driven `residential_street_suffix` to `circle|place|terrace` (`regions/exclusions.json`, tested, no hardcoded names, same discipline as #56); a re-ingest purges the 6 live nodes.
> - **Corroboration** (CDP-01) — wired in #54 but unsurfaced (no user value yet); honest single-source labels only.
> - **Defend-and-secure /plan** — #53 (rate-limit + obs); deploy caveat: `/health` 60/min collapses to one bucket behind Render's proxy — confirm probe + warm-ping cadence <60/min.

**Where that leaves Phase A (v17 read):** substantially paid down — the audit/identity trio (023/025/030) + deterministic ingest transport (036) + ingest-diff gate (027) landed; corroboration wired (#54); /plan defended (#53) and fast for returning visitors (039). **The four v12 near-term items are all resolved:** ① plan-reliability shipped as PR #65 (2026-07-01; verification note `docs/research/plan-reliability-verification.md`); ② R5 closed (2026-07-12 — cost model `docs/research/r5-cost-model.md` + live PlanMetrics ground truth); ③ dogfood-#2 done (2026-07-12 — Lane class confirmed closed, Circle/Place suffix gap fixed); ④ Epic 018 DONE (reconciled + six-state silence shipped Wave 3). **Remaining owner actions:** the air-row watch (self-healed 2026-07-13) and the PO decision queue below (FIRMS rotation + pip-audit-required were closed 2026-07-12 night).

**The strategic fork is RESOLVED (2026-07-14, Josh → Phase C, decision-log §47):** the next big lane is the **Phase-C pivot** — real auth (Supabase, signed off) + episode intake + history import, the "make it real for one real user" move. Phase B's ToS/consent + data-export/deletion line item is pulled forward as a hard precondition of history import; the rest of Phase B queues behind. Design-gated as before: 007 (readiness, needs a design session); 006 (novelty) unblocks via Epic 044's `been_on` producer. Parked at Phase E: B010 connectivity (`ready`), B001/B005 builds.

---

## 11-stage position

| Stage | Phase | Design | Build | Status |
|---|---|---|---|---|
| 0 Setup | 0 | ✅ | ✅ | Done |
| 1 Data sources | 0 | ✅ | ✅ | Done |
| 2 Schema/graph | 0 | ✅ | ✅ v0.2.0 | Done |
| 3 Corpus pipeline | 0 | ✅ | ✅ | Done — CorpusSource seam (012) + **enrichment loader (017)**; **first real region ingested** (Shenandoah-GWJ → Aura, 1458 trails + geometry + 3DEP elevation) |
| 4 Engine + cost | 0 | ✅ | ✅ | Built — **R5 CLOSED** (cost measured 2026-07-12, model `research/r5-cost-model.md`); TTL cache wired (Epic 013) |
| 5 Personalization | 1 | ✅ | ✅ | Belief pipeline (001), commons fork (010), outcome (002), context-assembly + `Episode.date` (003) all **DONE**; novelty (006) + readiness (007) remain |
| 6 Watch integration | 1 | ✅ | 🔶 | Device seam (004) done — **incl. the in-process Garmin poller (`watch_sync`, 55 tests)**; only the **always-on deployment** (R7) + **readiness filter (007, unwritten)** remain |
| 7 Eval deep-dive | 1 | ✅ | 🔶 | Methodology designed (on `main`); Epic 009 **IN_PROGRESS** — source-or-silence regression gate shipped (Wave 1, `evals/replay.py` + 5 golden scenarios + cassettes, hermetic); judge/Brier/N-run tiers deferred |
| 8 Multiplayer | 2 | ✅ | ❌ | Designed (on `main`); gated by always-on infra + auth provider (R3/R7) |
| 9 Commons | 3 | ✅ | 🔶 | Write half accreting (010 on `main`); read/aggregation dormant; **gated by T6** |
| 10 Experience/design-system | 4 | ✅ v0.1 | ✅ shipped, **live serving clean data** | design-system + **app UX** + **Maps (016/017)** + **GPX export (028)** + **latency waves (039/040)** on `main`; hosted Vercel→Render→Aura. **Corpus junk (dogfood #1) CLOSED** (#56 filter + re-ingest, verified on /plan); **cold-start eliminated** (#52 + Render Starter — see v18); **Epic 018 DONE** (six-state legible silence shipped Wave 3); dogfood-#2 done (2026-07-12). Remaining: personalization is anonymous-only until Phase-C auth; UX craft/conditions review lanes proposed (see `research/ux-review-*-2026-07.md`). Gate G1/G2 |
| 11 Native shell | 4 | ❌ | ❌ | Not started |

---

## Per-epic status (aggregated from index + live PRs/code)

| # | Epic | Status | Evidence / note |
|---|---|---|---|
| 001 | Belief update pipeline | **DONE ✅** | merged |
| 002 | Outcome card endpoint | **DONE ✅** | PR #18 — all 20 ACs tested (falsification-checked, not tautologies); all-None preference-marker drains safely. Minor: AC-5.2 stores *stripped* delta vs "raw" (whitespace edge untested) — non-blocking |
| 003 | Context assembly in `engine.plan()` | **DONE ✅** | PR #20 — `Episode.date` now written from `start_time` (R6 fixed); 18-month filter proven as unit + **live-DB E2E**. Minor: seeded-Josh E2E is split (live DB proves the date filter; Curator-enrichment half is fake-backed) — non-blocking |
| 004 | Device-integration seam | **DONE ✅** | merged (supersedes Garmin-only 004) |
| 005 | Valhalla drive-time | **DONE ✅** | absorbed into Epic 013 / PR #7 |
| 006 | Novelty filter in Curator | **DEFINED** | epic on `main`; depends on Epic 003 (done) + a `been_on` belief **producer** (unbuilt) + semantics decided |
| 007 | Readiness filter (Body Battery → Curator) | **NOT WRITTEN** | gap-audit M11: no epic file yet; depends on Epic 004 (done); solo-vs-party composition unspecified |
| 008 | API tests (`/plan` + `/health`) | **DONE ✅** | PR #57 merged — hermetic `/plan` + `/health` tests (provider + driver mocked); now the harness for the plan-reliability lane |
| 009 | Eval harness expansion | **IN_PROGRESS** | epic on `main`; source-or-silence regression gate shipped (Wave 1, `evals/replay.py` + 5 golden scenarios + cassettes); judge/Brier/N-run tiers deferred |
| 010 | Commons fork write | **DONE ✅** | PR #6 (remediates C1); doc-guard re-inverted now Epic 010 shipped (#32) |
| 011 | Scoped-write seam | **DONE ✅** | PR #6 (remediates C2) |
| 012 | CorpusSource seam | **DONE ✅** | PR #5 (remediates C5) |
| 013 | LiveAdapter seam | **DONE ✅** | PR #7 (remediates C6; +TTL cache, drive-time) |
| 014 | Overlay-egress + viewer-auth hardening (C3 + C4) | **DONE ✅** | PR #7 — C4 egress fix + **C3 interim edge auth** (`_authorize_viewer`: non-anonymous `viewer_id` needs `X-Dev-Viewer-Secret`, fails closed 403); full managed-auth deferred (R3) |
| 015 | CI Neo4j integration (live owner-isolation guardrail) | **DONE ✅** | PR #19 — **required** `integration (neo4j)` job runs live in GH Actions; read+write isolation + a real falsifiability test (removing `owner_scope` reds it). Fast legs stay DB-free |
| 016 | Maps & terrain (topographic Detail map · route · elevation) | **DONE ✅** | #38 — MapLibre topo map + assembled route + trailhead + card glyph + elevation chart. Code-split (feed stays map-free), honest empty/failure states, attribution. Built on mock, swaps to live at the contract |
| 017 | Terrain elevation enrichment (USGS 3DEP profiles) | **DONE ✅** | #39 — enrichment loader (closed the "no graph write yet" gap) + 3DEP sampler → parallel-array store → API on the feed card. Pre-merge review caught + fixed the cross-lane contract drift (snake_case + card placement) before `main`. Real data: 1458 trails carry a 10 m profile in Aura |
| 018 | Live conditions on the card | **DONE ✅** | reconciled 2026-07-12 + six-state legible silence shipped (Wave 3, PR #162) — `present · stale_degraded · no_hazard · no_data · unavailable · not_fetched`; gate-defended in eval-replay |
| 022 | Duration truth | **DONE ✅** | CoMaps borrow — wired Naismith estimate through the live feed with render-layer disclosure |
| 023 | Agency length | **DONE ✅** | authoritative USFS/NPS length capture + NPS multi-part trail re-assembly |
| 024 | Schema format | **DONE ✅** | integer `schema_format` on `:Meta` + API startup gate via /health disclose |
| 025 | Validity exclusions | **DONE ✅** | geometry/null-island validity in the load path + externalized trail-filter denylists to config |
| 027 | Facet ingest-diff | **DONE ✅** | per-facet leveled ingest-diff check (within-run) + stats on /health |
| 028 | GPX export | **DONE ✅** | `GET /trail/{id}/export.gpx` + Send-to-device on Detail; live endpoint verified serving well-formed GPX 1.1 |
| 030 | Slug collision | **DONE ✅** | guard the unguarded short-slug canonical_id collision + re-runnable audit (the Phase-A landmine — closed) |
| 031 | GPX reader | **DONE ✅** | GPX reader-tolerance module (dedupe · <2pt-drop · timestamp-derived-flag) |
| 032 | Grade-aware ETA | **DONE ✅** | port Valhalla's MIT grade-speed curve; per-segment integral replaces flat Naismith |
| 033 | OpenTopoData fallback | **DONE ✅** | OpenTopoData fallback ElevationSampler (degrade-and-disclose network elevation transport) |
| 034 | NPS closures adapter | **DONE ✅** | NPS closures live adapter (new `ConditionKind.closures`; nearest-park Closure/Danger alerts) |
| 035 | OSM water overlay | **DONE ✅** | OSM on-trail water-source overlay (fetch + persist + proximity read; no potability claim) |
| 036 | pyosmium transport | **DONE ✅** | deterministic OSM PBF transport (pyosmium; additive, default not flipped) |
| 037 | Open-Meteo failover | **DONE ✅** | weather availability-failover spike (specs failover + spread-disclosure); downstream build BLOCKED on commercial-license PO decision |
| 038 | Trail search geocoder | **DONE ✅** | trail-name search + geocoder seam (B001) — spec-complete spike, no product code |
| 039 | Feed first-paint latency | **DONE ✅** | Wave 1 (anonymous fast-path · engine-layer anon plan cache · frontend stale-while-revalidate); returning visitor paints 0.385s |
| 040 | Two-phase render | **DONE ✅** | graph-only ranked cards <1.5s · `POST /plan/conditions` patch · six-state pending silence (merged overnight train, PR #175) |
| 041 | Water overlay read surface | **DONE ✅** | Detail answer line + quiet map markers; CDP-02 three-way silence (PR #182) |
| 042 | Manual trip log (learning-loop fallback) | **BACKLOG** | "I did this" button on saved trails → manual Episode + Outcome flow, watch-free (GLM IA review §6.1; Rule #6) |

**Merged PRs (baseline + docs + waves):** remediation/seam set #5/#6/#7/#9/#10 · UI #14/#17/#22 · overnight batch #18/#19/#20/#21 · R10 secret #25 · roadmap+runbook #26 · **doc overhaul #27/#28/#29/#30** · commons doc-guard #32 · **Maps #38/#39** · **hosting #40/#41** · corpus filter #56 · API tests #57 · plan-reliability #65 · **OSS-borrow wave #112–#155** (022–036, 028) · **latency waves #157/#160/#162/#164/#175** (039/040, 018 silence) · **security remediation #166** · water overlay #182 · UX craft/conditions lanes #174/#179 · plus the roadmap/workflow-lint/dependabot housekeeping PRs.

---

## Live in-flight

Phase-1, the baseline, the docs overhaul, Maps, **the OSS-borrow wave, the latency waves, and the security remediation are all merged and live.** The app is hosted end-to-end (Vercel → Render → Aura) and serving real data; the current push is the UX craft/conditions review lanes + the owner decision queue (Phase C auth, ToS/consent, Open-Meteo license, readiness design).

### ◆ Maps & terrain — SHIPPED ✅ (Epics 016 + 017)
Built dogfood-first ("no maps, I can't see shit") as two parallel lanes that converged at the elevation chart. **016 (frontend, #38):** MapLibre topo Detail map + assembled route + trailhead + card glyph + elevation profile, code-split so the feed stays map-free, with honest empty/failure states + attribution. **017 (backend, #39):** the enrichment **loader** (closed the long-standing "no graph write yet" gap) + USGS-3DEP sampler → parallel-array store → exposed on the feed card. The lanes were coupled only by a frozen wire contract; a **pre-merge adversarial review caught the one real risk — the two lanes had drifted on field casing + placement — and reconciled it on #39 before it hit `main`** (`tests/test_maps_contract.py` now locks the API shape to the frontend type).

### ◆ Live-data path — DONE ✅ (R7 API-hosting resolved)
Real data serves end-to-end on the phone: the **Shenandoah-GWJ region is loaded into Aura and served live** (corpus counts live in `STATUS.md` — refreshed by `scripts/gen_state.py`; per-segment geometry + 10 m 3DEP elevation, verified honest, full provenance, no fabricated curves). The hosting picture (R7 API path) is resolved:
- **Frontend = Vercel** (live) · **DB = Aura Free** (loaded) · **API = Render Starter (since 2026-07-13; was Free — v18)** — deploy config (Docker + `render.yaml` + default-deny CORS, #40) + Aura-TLS CA hardening (#41) merged; deployed and no longer idles.
- **DONE:** API deployed to Render, Vercel env flipped (`VITE_USE_MOCK=false`, `VITE_API_BASE_URL=<render>`, viewer secret) → real Shenandoah trails on the phone; dogfood #1 (junk trails) closed; cold-start eliminated by the always-on upgrade. **Transient-500 hardening shipped** (PR #65, 2026-07-01 — driver liveness + bounded retry + provider transient-retry; verification note `docs/research/plan-reliability-verification.md`).
- **Gotcha resolved (#41):** a Python CA-path gap broke the `neo4j+s://` TLS handshake to Aura ("Unable to retrieve routing information"); fixed in app + container via certifi/`ca-certificates`, **strict TLS preserved** (no `+ssc`/trust-all), with a test guarding against future downgrades.
- **Ingest follow-ups (none block the demo):** verify the 1643→1458 `canonical_id` slug merge (~185 collapsed — same-trail or collision?); drop the seeded Old Rag duplicate (`ct:old-rag-loop`); 15/24 trailheads are unlinked (OSM sparsity, not a bug). *(`scripts/apply_schema.py` is now tracked, with `tests/test_apply_schema.py` — no longer a follow-up.)*
- **Still deferred:** the always-on **watch-poller** host (Garmin sync) — not needed to browse trails.

### ◆ Baseline promotion — DONE ✅
Trunk promoted to `main` (`725c442`) · R10 secret fix (#25) · repo → `joshcrowbuild` Team org · default branch → `main` · **branch protection** (7 required checks incl. `integration (neo4j)` + `docs-lint`, no force-push/delete) · tag `v0.1-phase1` cut (`c12be36`) · merged feature branches swept. Nothing outstanding.

### ◆ Documentation overhaul — COMPLETE ✅ (#27–30)
Four waves, one PR each: **(1) Freshness** — killed the stale "current position" in CLAUDE.md/README/decision-log + 6 research banners (point at roadmap as the status SSOT). **(2) Navigation** — new `docs/README.md` map + `docs/research/README.md` index (25 docs classified) + wired roadmap into CLAUDE.md. **(3) Dedupe/archive** — `git mv` ~115KB closed audits → `docs/research/archive/`; folded decision-log-additions §32–40 into the decision log; single-sourced git/PR hygiene into `development-process.md`. **(4) Anti-drift** — `scripts/gen_epic_index.py` + `scripts/doc_lint.py` + the CI `docs-lint` job (denylist · broken-link · epic-index-sync). Result: a lean hot path, a findable map, and a gate that fails on wrong memory. *PM follow-ups closed: `docs-lint` added to branch protection; this v7 reconciliation.*

---

## Dependency graph & next-up per lane

```
DONE & BASELINED ─────────────────────────────────────────────────────────
  ✅ Phase-1 backend (001–005, 010–015) · ✅ app UX · ✅ R10 secret · ✅ docs overhaul
  ✅ v0.1-phase1 tag · ✅ commons doc-guard (#32) · ✅ Maps (016/017, #38/#39)
  ✅ OSS-borrow wave (022–036, 028) · ✅ latency waves (039/040) · ✅ security remediation (#166)
  ✅ Epic 018 six-state silence (Wave 3) · ✅ Epic 041 water overlay read surface (#182)

LIVE ✅ — real-data path complete (R7 API-hosting done)
  ✅ frontend = Vercel · ✅ DB = Aura (counts live in STATUS.md) · ✅ API on Render Starter (v18) · ✅ Vercel flipped
  ✅ real trails on the phone · ✅ dogfood #1 closed (#56 + re-ingest) · ✅ cold-start eliminated
  ✅ plan-reliability shipped (#65) · ✅ R5 cost closed (2026-07-12)

BEHIND IT — the design-gated Phase-1 remainder
  Epic 006 (novelty)    ── DEFINED, needs a been_on PRODUCER (unbuilt) + semantics decided
  Epic 007 (readiness)  ── needs a design session (safety-adjacent); NO epic file yet
  Epic 009 (eval)       ── IN_PROGRESS; source-or-silence gate shipped, judge/Brier/N-run deferred

OTHER OPEN
  Watch-poller host       ── the one still-deferred piece of R7 (Garmin sync; not needed to browse)
  UX craft/conditions     ── review lanes proposed (research/ux-review-*-2026-07.md); await green-light
  Epic 042 (manual log)   ── BACKLOG; the learning-loop fallback for users without watch integration

PARKED — UX RETHINK (GLM IA review findings, 2026-07)
  Omnibox / Unified Intent ── promote parse-intent + B001 geocoder to Home header; kill origins.ts enum
  Feed-level map toggle     ── plot curated cards on a map; requires MapLibre on Home (currently Detail-only)
  Curator's Note on card    ── promote fitLine from Detail to feed card; Rule #1 guardrails required
```

**Owner manual queue (Josh) — RECONCILED 2026-07-12 after the ops day.** Done ✅: warmer decision (OFF) · TTL/workers flips · branch protection (12 checks) · Vercel bundle verified · agency keys ×4 + full adapter list · PlanMetrics read (R5 closed). Remaining:
1. **Rotate the FIRMS map key** (5 min, firms site + Render env): the old key sat in pre-#166 Render logs. Low risk (free key), pure hygiene; the redaction filter protects the new one.
2. **Add `pip-audit` as the 13th required check** (1 min, laptop): it flipped to blocking in #166 — making it *required* closes the loop.
3. **Watch the air row**: AirNow read `unavailable` on first probe (28s upstream stall, honestly disclosed). If still unavailable after a day, check the AirNow key-activation email.
4. **Decisions when ready:** ① Supabase auth sign-off (gates Phase C) · ② 15-min ToS/privacy policy-positions chat (gates the last Phase-B lane) · ③ Open-Meteo commercial license (gates Epic 037 downstream) · ④ readiness (007) design session (Phase D).
5. **Optional/later:** Sentry + uptime accounts · Aura console node-count check (sizes the runway to the Free-tier cliff).

**Next-up by lane (v20):**
- **Build lane:** Phase C is unlocked. Order: ① **Epic 043 managed auth** (buildable now); ② **Epic 042 manual trip log**; ③ **ToS/consent + data-export/deletion scaffold** (after the policy-positions chat — gates 044); ④ **Epic 044 history import**; ⑤ **Epic 006 novelty** (via 044's `been_on` producer). The UX-vision adoption lanes (hero "Call" card · desktop map-split · region-conditions probe) can run in parallel.
- **PO decisions (Josh, in unlock order):** ① **the 15-min ToS/privacy policy-positions chat** (gates the deletion scaffold → 044); ② **geocoder provider** (Open Decision #3 — gates place search); ③ **Open-Meteo commercial license** (unblocks Epic 037's downstream build); ④ **readiness (007) design session** (safety-adjacent, Phase D).
- **Parked, on purpose:** B010 connectivity (`ready`, Phase E) · B001 search build (spike done, Phase E) · B005 route drawing (Phase E) · commons read half (Stage 9, gated on T6/R1) · Epic 009's deferred halves (LLM-judge, Brier hook, N-run tiers — after 006/007 exist to evaluate).
- **UX rethink queue (from GLM IA review):** ① **Omnibox** — elevate `parse-intent` free-text input to `Home` header + wire B001 geocoder to resolve arbitrary place names (kills the fixed `origins.ts` enum; biggest UX unlock identified). ② **Feed-level map** — toggle on Home to plot curated cards relative to each other and origin. ③ **Curator's Note** — promote `fitLine` to the feed card (Rule #1 guardrails: text must be derivable from verified facts, never LLM-generated prose). ④ **Epic 042** — manual trip log (BACKLOG, Phase C).

---

## ◆ The overnight batch — COMPLETE & verified (2026-06-26)

All four landed and merged; a 6-reviewer adversarial pass confirmed each DoD is genuinely met. Full `make check` on the merged tree: **516 passed / 8 neo4j-deselected, ruff + mypy clean; all CI jobs green** incl. the live `integration (neo4j)`. No merge-skew across the four near-simultaneous merges.

1. ✅ **Epic 002 (outcome)** — #18. All 20 ACs tested (falsification-checked). *Minor:* AC-5.2 stores *stripped* delta vs "raw" (non-blocking).
2. ✅ **Epic 015 (CI Neo4j)** — #19. Required `integration (neo4j)` job ran live in GH Actions; falsifiability real.
3. ✅ **Epic 003 + `Episode.date`** — #20. R6 fixed; 18-month filter proven unit + live-DB E2E. *Minor:* E2E split (Curator-enrichment half fake-backed).
4. ✅ **Docs reconcile (R4)** — #21 + UI commits. Design docs on `main`; README union preserved DONE statuses; links resolve.

**Still held back (need a human design pass):** Epic 007 readiness (safety-adjacent) · Epic 006 `been_on` producer.

---

## Open risks & cross-cutting threads

| ID | Risk / thread | State | Action / owner |
|---|---|---|---|
| **R1** | **T6 licensing/consent gate.** ODbL `separable` flag has zero code readers (gap-audit m2); best-view cache blends sources → Derivative-DB risk; Valhalla adds unattributed OSM. Commons consent substrate (opt-in + salt) landed with Epic 010, but the public-release ODbL handling is unresolved. | **OPEN** — write half accreting now | **Gates Stage 9 public release.** Enforce separability invariant + ODbL posture before any public commons. |
| ~~**R2**~~ | ~~No live-Neo4j test coverage.~~ | **RESOLVED — Epic 015 (#19)** | Required `integration (neo4j)` job proves read+write isolation + falsifiability against a real DB, live in CI. |
| **R3** | **C3 auth — interim built, full auth deferred.** Epic 014 S3 added an edge guard (`_authorize_viewer`): any non-anonymous `viewer_id` must present `X-Dev-Viewer-Secret` (constant-time compare) or gets HTTP 403 — fails closed. But at the query layer `ScopedSession` still trusts `viewer_id` verbatim, so a secret-bearing forged id is still a forged write; the dev-secret is a single shared interim credential, not per-user auth. | **PARTIAL** — interim guard built; full auth deferred | Decide managed auth provider (Supabase/Clerk/Auth0 — undecided) at Stage 8; until then the shared dev-secret is the only gate. |
| ~~**R4**~~ | ~~Trunk ↔ design-branch doc divergence (13 docs).~~ | **RESOLVED — #21 + docs overhaul** | All design docs on `main`; the docs overhaul (#27–30) then indexed + drift-guarded the whole surface. |
| **R5** | **Latency half MEASURED (Epic 039, 2026-07-08):** /plan was 8.8s cold / 3.7–4.0s on every load; after Wave 1 (anon fast-path · engine-layer anon plan cache · frontend stale-while-revalidate) a returning visitor paints in **0.385s** and a cache hit serves in **0.22–0.73s** (verdict table: `docs/research/feed-first-paint-latency-wave1.md`). Cache-hit calls also spend zero LLM tokens (PlanMetrics `feed_cache_hit`), so the *cost*-per-session half is now partially mitigated AND measurable from Render logs. | **CLOSED (v17)** — latency measured (039) + cost measured (2026-07-12) | Model: `research/r5-cost-model.md`. Ground truth from live PlanMetrics: **~$0.0016/cold plan** (Sonnet judge + Haiku intent; estimator undercounts prompt tokens — order of magnitude stands); warmer OFF ⇒ organic-only LLM ≈ $1–2/mo; hosting $7; APIs $0. Residual watch item (not a risk): the Aura Free→Pro cliff (+$66/mo) when regions grow — escape hatch is self-hosting (cost model §8). Cold path 7.2s/4.7s post-flips; Epic 040 owns the rest. |
| ~~**R6**~~ | ~~M1 `Episode.date` never written.~~ | **RESOLVED — Epic 003 (#20)** | `upsert_episode` SETs `e.date` from `start_time`; 18-month filter proven by a live-DB E2E. |
| **R7** | **Hosting/compute — API path RESOLVED.** Frontend = **Vercel** (live). DB = **Neo4j Aura Free** (16 regions, ~5% of caps; counts live in `STATUS.md`). API = **Render Starter (0.5 vCPU, upgraded 2026-07-13 — the instance had in fact been Free/0.1 vCPU despite v11's "always-on paid" note; corrected in v18)**. Spin-down and the CPU-bound probe fan-out both resolved (#185 + Starter). Deferred: the always-on **watch-poller** host (Garmin sync, Epic 004) + Stage-8 multiplayer compute. | **RESOLVED (API)** — watch-poller still deferred | Aura Free idle-closes connections → driver resilience shipped (PR #65 — liveness + bounded retry); watch Aura caps/idle if usage grows. Stand up the watch host when Garmin sync is needed. |
| ~~**R8**~~ | ~~CI `workflow-lint` red trunk-wide.~~ | **RESOLVED — #12** | `actionlint` now runs via its official download script; `workflow-lint` green. |
| ~~**R9**~~ | ~~PR #22 (UI) targets `main`, not trunk.~~ | **RESOLVED** | Retargeted + merged; honesty invariants verified. The contract-check surfaced R10. |
| ~~**R10**~~ | ~~HTTP-adapter dev-viewer-secret gap (guaranteed 403 on live calls).~~ | **RESOLVED — #25** | `httpPlanner.ts` now injects `X-Dev-Viewer-Secret` from `VITE_DEV_VIEWER_SECRET` for non-anonymous viewers (omitted for anonymous; tested). Live-data wiring is unblocked; secret stays in `.env` (Rule #10). |

### Thread tracker (T1–T7 — mirrors workplan; the M9 fix)

- **T1 · Infra/secrets/CI.** ✅ `workflow-lint` fixed (#12); FIRMS key log-leak fixed (#9); **live-Neo4j CI guardrail enforced (Epic 015)**; **doc-drift now CI-guarded (`docs-lint`, #30)**. Still open: the `SecretProvider` seam (`.env` plaintext only — gap-audit M6).
- **T2 · Access-control-at-query-layer.** ✅✅ reads + writes seamed (Epic 011); Outcome-endpoint bypass closed (#9); **the invariant is proven end-to-end against a live Neo4j in CI on every PR (Epic 015)** — a forgotten owner clause reds the build. (Forged-identity auth is still R3.)
- **T3 · Forked commons write.** ✅ built (Epic 010), accreting born-severed observations. Read/aggregation dormant to Stage 9. (Doc-guard cleanup in flight — build lane.)
- **T4 · Evaluation.** 🔶 truthfulness harness exists; **source-or-silence regression gate shipped** (Epic 009 Wave 1, `evals/replay.py` + 5 golden scenarios + cassettes, hermetic, CI-gated); deferred halves (LLM-judge, Brier hook, N-run tiers) remain unbuilt.
- **T5 · UX.** ✅ design-system v0.1 + the **personal-intelligence app UX** (Home/Detail/Tuning/Outcome + Confidence/Staleness honesty primitives, verified honest) + **Maps (016/017): topo map, route, real elevation**. Live on real Aura data (Render always-on + Vercel flipped); dogfood #1 closed.
- **T6 · Legal/licensing/consent.** ⚠️ see R1 — gates Stage 9 public release; separability invariant unenforced.
- **T7 · Naming/branding.** Working title "Adventure Planner"; anytime.

---

## Lane discipline (this doc's owner)

The **Product Owner** persona (formerly the "PM/planner lane") owns `docs/process/roadmap.md` + `docs/workplan.md`; `docs/epics/README.md` is read-mostly (generated by `gen_epic_index.py`). Never touch `api/`, `graph/`, `orchestration/`, `ingestion/`, `frontend/`, or another lane's epic spec. Branch off freshly-fetched `origin/main` in a dedicated worktree (rule G3); small commits; **PRs into `main`** (the baseline). Full personas, grounding rules (G1–G4), and handoffs: `docs/process/agent-operating-model.md` (PO-owned).
